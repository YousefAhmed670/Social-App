export enum SYS_ROLE {
  User = "user",
  Admin = "admin",
  SuperAdmin = "superAdmin",
}
export enum GENDER {
  Male = "male",
  Female = "female",
}
export enum USER_AGENT {
  Local = "local",
  Google = "google",
}
export enum TOKEN_TYPE {
  Refresh = "refresh",
  Access = "access",
}
